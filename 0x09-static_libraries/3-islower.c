#include "main.h"

/**
 * _islower - will check for lowercase character
 * @c: char to check
 * Return: 1 if c is lowercase and 0 if it is not.
 */

int _islower(int c)

{
	return (c >= 'a' && c <= 'z');
}
